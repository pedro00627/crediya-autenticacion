server:
  port: 8080
spring:
  application:
    name: "Autenticacion"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
adapters:
  r2dbc:
    host: localhost
    port: 5432
    database: postgres
    schema: crediya
    username: postgres
    password: P1o2s3G4.
logging:
  level:
    # 1. Nivel por defecto para todo: solo mostrar advertencias y errores.
    root: WARN
    # 2. Nivel para tu aplicación: aquí sí queremos ver los logs de INFO y DEBUG.
    co.com.pragma: INFO
    # 3. Nivel para componentes específicos de Spring (opcional, pero recomendado).
    org.springframework.web: WARN
    io.r2dbc: WARN